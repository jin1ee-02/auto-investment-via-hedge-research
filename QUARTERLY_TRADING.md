# 상시 실행형 리서치·매매안 계약

확인일: 2026-09-14. 분기 공시는 거래 일정이 아니라 최신 후보 데이터입니다. 사용자가 원할 때마다 웹에서 새 파이프라인을 시작할 수 있습니다.

## 파이프라인 계약

- 기본 후보 조건은 모든 방향, 같은 방향 최소 2개 펀드, 혼합 허용입니다.
- 후보 순위는 `max(확대 펀드 수, 축소 펀드 수)` 내림차순, 방향 일치율 내림차순, 종목 키 오름차순입니다. 실행 시 1~8개(기본 6개)를 선택해 조사합니다.
- 리서치는 직렬 실행합니다. 하나라도 실패하면 완료된 부분 결과는 보여주지만 계좌 주문안과 승인은 만들지 않습니다.
- 완료 보고서의 캐시 키에는 공시·선택 펀드·모델·날짜·조사 방식이 포함됩니다.
- 매수·매도 판단은 TradingAgents 최종 신호와 별도 종합 AI 판단이 모두 일치해야 합니다. 점수 60, 신뢰도 65%, 데이터 누락 없음도 필수입니다.
- 확대 우세는 매수만, 축소 우세이면서 현재 미국주식 보유인 경우는 매도만 검토합니다. 나머지는 유지합니다.

## 계좌와 수량 계약

- 토스 종합매매 계좌가 하나면 계좌 목록 조회로 자동 선택합니다. 여러 계좌면 `TOSS_ACCOUNT_SEQ`를 요구합니다.
- 미국 투자 자본은 USD 현금 매수가능금액과 미국주식 현재 평가액의 합입니다. KRW와 국내주식은 자본·주문 대상에서 제외합니다.
- 후보가 아닌 미국 보유와 게이트를 통과하지 않은 보유는 보호합니다. 한도를 넘는 기존 보유를 강제 청산하지 않습니다.
- 배분 점수 비례 목표에 종목 20%, 현금 10%, 회전율 30%, 최소 주문 $100, 비용 여유 1%를 적용합니다.
- 지정가 DAY 정수 주문만 만듭니다. 공매도, 신용, 환전, 소수점 주문은 없습니다.
- 매도대금은 체결되어 새 계좌 스냅샷에 반영되기 전까지 매수 재원이 아닙니다.
- 미체결 주문이 하나라도 있거나 USD 시세가 5분보다 오래되면 주문안은 차단합니다. 리서치와 목표안은 유지되며 새 시세·계좌로 다시 실행할 수 있습니다.

## 승인과 중복 방지

- 각 실행에는 고유 `runId`와 `batchId`가 있습니다. 원장은 `계좌 + batchId`로 저장하므로 같은 분기 재실행이 가능합니다.
- 이전 batch가 전송 중이거나 응답 확인 불가이면 새 실행을 막고 조회로 해소합니다.
- 승인 보고서는 계좌, 리서치, 목표, 가격, 수량, 설정 전체의 해시이며 15분 후 만료합니다.
- 웹 승인 센터는 검토와 JSON/HTML 보고서 표시만 합니다. 주문 API는 노출하지 않습니다.
- 로컬 대화형 터미널에서 정확한 `APPROVE <reviewId>`를 입력하고 `TOSS_ENABLE_LIVE=true`여야 주문 POST가 가능합니다.
- 주문 의도는 첫 POST 전에 SQLite에 기록합니다. 응답을 잃은 주문은 자동 재전송하지 않습니다.

```powershell
# 리서치 + 토스 조회 + 승인 보고서 생성. 주문 없음.
.venv/Scripts/python.exe -X utf8 -m pipeline.run --broker toss

# 사용자가 보고서를 검토한 뒤에만 실행.
.venv/Scripts/python.exe -X utf8 -m pipeline.run --broker toss --approve REVIEW_ID

# 주문 상태 읽기 전용 대조.
.venv/Scripts/python.exe -X utf8 -m pipeline.run --broker toss --reconcile

# 새 주문 전송 비상 차단.
New-Item -ItemType File work/STOP_TRADING
```

`--watch`와 무인 `--live`는 의도적으로 지원하지 않습니다. 공식 계약은 [토스증권 OpenAPI 1.2.17](https://openapi.tossinvest.com/openapi-docs/latest/openapi.json)을 기준으로 합니다.
