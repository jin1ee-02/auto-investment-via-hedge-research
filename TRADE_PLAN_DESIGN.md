> 역사 자료: 아래는 구현 전 제안입니다. 현재 상시 실행형 동작은 [QUARTERLY_TRADING.md](QUARTERLY_TRADING.md)를 우선 참고하세요.

# 보유 계좌 기반 매매안 설계 (구현 제안)

## 현재와 다음 단계
현재 구현은 공통 매매 후보 → TradingAgents 보고서 → 조건을 통과한 종목의 가상 목표 비중이다. 계좌 보유량, 주문 가능 현금, 미체결 주문을 반영하는 리밸런싱과 실주문은 미구현이다. 아래는 후속 구현의 계약이며 현재 실행 가능한 기능이 아니다.

## 조사 대상
신규 매수 후보와 현재 보유 종목의 합집합을 티커/시장 단위로 중복 제거한다. 보유 종목은 공통 펀드 매매 조건을 충족하지 않아도 위험 재평가 대상으로 포함한다. 현재 researchCandidates 및 run_research의 공통 매매 검증을 그대로 사용하면 보유 종목이 누락되므로, 검증된 계좌 종목을 허용하는 별도 경로가 필요하다. 보고서에는 후보 출처와 계좌 무관 투자 판단을 저장한다.

신규 후보는 유동성·최근 가격·공시 신선도·예정 이벤트로 사전 선별한다. 펀드 수는 조사 우선순위 근거이지 기대수익률이 아니다. 보유 종목 중 철회 조건에 접근했거나 예정 이벤트가 있는 종목을 먼저 재조사하고, 이후 신규 후보를 비용 한도 안에서 조사한다. 데이터·모델·조사 버전·유효기간이 같은 보고서는 재사용한다.

## 두 종류의 판단
1. 종목 보고서: 투자 논지, 2~6주 촉매, 위험, 미확인 근거, 관찰/진입/철회 조건과 출처. 현재 buy/watch/avoid는 이 단계의 의견이다.
2. 계좌 매매안: 보고서 합계와 계좌 스냅샷을 받아 신규 매수/추가 매수/유지/일부 축소/청산/보류를 제안한다. 종목별 현재 비중과 목표 비중, 변경 이유, 근거 보고서 ID, 유효기한을 명시한다. 보고서의 avoid를 무조건 청산으로 변환하지 않는다.

## 수량 결정
AI는 근거와 목표 비중 초안을 만든다. 서버가 순자산, 통화, 현재 호가, 손실 예산, 종목·업종 집중도, 최소 현금, 회전율 상한을 적용해 목표 수량과 차이를 계산한다. 점수 60 또는 신뢰도 65%를 기대수익률이나 최적 비중으로 해석하지 않는다.

예: 보유 20주, 검증된 목표 12주이면 8주 축소 제안. 단, 매도 가능 수량·동일 종목 미체결 주문을 대조한 뒤 확정한다. 목표 수량이 0이라는 이유만으로 조사 누락 종목을 청산하지 않는다. 근거 미확인 시 유지/보류하고 기존 위험 한도 위반을 별도 표시한다.

매수 금액은 현재 주문 가능 금액에서 예약된 금액과 비용 여유분을 차감한 범위로 제한한다. 예정 매도 대금은 체결·계좌 반영을 확인하기 전 매수 재원으로 사용하지 않는다. 정수 수량/소수점 지원, 최소 주문 단위, 환율, 수수료를 명시적으로 검증한다.

## 토스 연결
공식 문서 확인: 2026-09-12, https://developers.tossinvest.com/

공식 API의 계좌/보유 종목, 매수 가능 금액, 판매 가능 수량, 미체결 주문, 현재가·호가, 수수료·환율 조회로 스냅샷을 만든다. Client Credentials 인증과 계좌 헤더를 서버에서 관리하고 허용 IP가 고정된 실행 서버를 사용한다. 프런트엔드 또는 AI 프롬프트에 자격 증명을 전달하지 않는다. 실제 연결 시 최신 스키마와 계좌별 권한·거래 제한을 다시 검증한다.

실행 상태는 draft → validated → approved → submitted → partially_filled/filled/cancelled/rejected/unknown 으로 영속 저장한다. 승인·실행 직전에 계좌와 시세를 다시 검증하고 내용이 달라지면 새 안을 만든다. clientOrderId로 중복을 막고 응답 불명은 조회로 해소하며 무조건 재전송하지 않는다. 부분체결·취소·수동 주문을 다음 계좌 스냅샷과 대조한다.

## 개발 순서
1. 읽기 전용 계좌 연결 또는 수동 스냅샷 입력, 보유 종목 조사 경로.
2. 계좌를 반영한 목표 비중 및 매수/축소 수량 모의안, 변경 이유와 검증 결과 화면.
3. 실제 시세로 모의 체결·부분체결·재시도·중복 방지 테스트 및 성과 기록.
4. 사용자가 확인한 매매안의 제한된 실행. 이후 사용자가 설정한 금액·회전율·손실 한도 안에서 자동화하고 즉시 중단 기능 제공.

다음 구현 단위는 1~2다. 신규 매수뿐 아니라 현재 보유 종목의 일부 축소까지 같은 화면에서 비교 가능해야 계좌 기반 매매안이 완성된다.
