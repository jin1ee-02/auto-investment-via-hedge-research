# 계좌 기반 매매안 설계

확인일: 2026-09-15. 이 문서는 현재 구현된 계좌 매매안의 설계와 아직 구현하지 않은 경계를 설명합니다. 운영상 정확한 게이트는 [QUARTERLY_TRADING.md](QUARTERLY_TRADING.md)를 우선합니다.

## 입력

계좌 매매안은 다음 입력을 결합합니다.

- 선택 펀드와 후보 조건
- 완전한 최신·직전 13F 스냅샷에서 계산한 공통 확대·축소 후보
- 선택된 모든 후보의 완료된 TradingAgents 보고서
- 토스의 미국주식 보유, USD 매수가능금액, 미체결 주문, 현재가와 매도 가능 수량
- `pipeline/strategy.json`의 운용 제약

후보 하나라도 리서치가 실패하거나 공시·펀드 컨텍스트가 맞지 않으면 부분 결과로 계좌 매매안을 만들지 않습니다.

## 종목 판단

TradingAgents Portfolio Manager의 최종 등급만 AI 방향으로 사용합니다. 별도의 종합 AI 호출이나 신뢰도 65% 게이트는 없습니다.

- 확대 우세 + Buy/Overweight: 매수 검토
- 축소 우세 + Sell/Underweight + 현재 미국주식 보유: 매도 검토
- 그 외: 유지

종목·기업 정체성 불일치, 잘못된 티커, 상장폐지, 거래정지 또는 시장 데이터 확인 불가처럼 자동 매매 제외로 표시된 무결성 문제는 주문을 차단합니다. 일반적인 연구 한계는 표시하되 자동 차단 사유로 사용하지 않습니다.

현재 리서치 집합은 13F 공통 변화 후보만 포함합니다. 후보가 아닌 기존 보유를 별도로 AI 재조사하지 않으며, 이 종목들은 보호 보유로 남습니다.

## 자본과 목표 배분

미국 투자 가능 자본은 다음과 같습니다.

```text
USD 매수가능 현금 + USD 표시 미국주식 평가액
```

KRW, 국내주식 및 기타 비미국·비USD 자산은 자본과 주문 대상에서 제외합니다.

매수 후보의 배분 점수는 다음과 같습니다.

```text
TradingAgents 등급 강도 × 같은 방향 펀드 수 ÷ 선택 펀드 수
```

Buy는 100, Overweight는 80입니다. 보호 보유와 최소 현금을 먼저 제외하고 남은 금액을 점수 비례 water-fill 방식으로 배분하되 종목별 최대 비중과 최대 보유 수를 적용합니다.

- 기존 보유가 종목 수나 비중 한도를 초과해도 강제 청산하지 않습니다.
- 기존 한도 위반 상태에서는 신규 포지션을 막습니다.
- 매수 신호 보유의 계산 목표가 현재 평가액보다 낮아도 자동 축소하지 않습니다.
- 매도 신호 보유는 목표 0으로 계산하되 실제 주문은 매도 가능 수량 이내로 제한합니다.
- 매도 예상대금은 같은 제안의 매수 재원으로 사용하지 않습니다.

현재 한도는 종목당 100%, 최소 현금 0%, 회전율 100%, 최소 주문 $0, 비용 여유 1%, 최대 보유 6개입니다. 완화 전 표준값은 `pipeline/strategy.standard.json`에 보존되어 있습니다.

## 주문 수량

서버는 목표 금액과 최신 가격으로 목표 정수 주식 수를 계산한 뒤 현재 정수 보유 수량과의 차이를 주문 수량으로 사용합니다.

- 주문 유형: LIMIT
- 유효 기간: DAY
- 통화: USD
- 수량: 정수
- 가격: 최신 가격을 센트 단위로 내림

미체결 주문이 있거나, 관련 시세가 5분보다 오래됐거나, 가격이 $1 미만이거나, 계획과 현재 계좌 가치가 `max($1, 2%)`보다 크게 달라지면 주문안 생성을 차단합니다. 매수는 현재 USD 현금과 비용 여유 안으로, 매도는 현재 매도 가능 수량 안으로 제한합니다.

## 승인과 실행

주문이 있으면 계좌, 계획, 설정, 주문 전체를 해시한 15분짜리 승인 보고서를 만듭니다. 웹은 보고서 표시와 다운로드만 제공하며 실행 권한이 없습니다.

실행은 로컬 대화형 터미널에서 다음 조건을 모두 만족해야 합니다.

- 정확한 `APPROVE <reviewId>` 입력
- `TOSS_ENABLE_LIVE=true`
- `work/STOP_TRADING` 부재
- 승인 미만료
- 재계산된 계좌·계획·설정·주문 해시 일치
- 미해결 이전 배치 없음

첫 주문 POST 전에 SQLite에 전체 주문 의도를 기록합니다. 주문은 보내기 직전에 `unknown`으로 표시하고, 확인 응답을 받으면 `submitted`와 주문 ID를 저장합니다. 오류나 응답 유실 시 배치를 `needs_reconciliation`으로 남기고 자동 재전송하지 않습니다.

## 현재 구현하지 않는 범위

- 미조사 기존 보유의 독립적인 AI 위험 재평가
- 업종 집중도와 종목별 손실 예산 기반 배분
- 시장가, 소수점, 신용, 공매도, 자동 환전 주문
- 무인 또는 일정 기반 자동 주문
- 응답 불명 주문의 자동 재전송
- 부분체결 후 잔량 자동 재주문·취소·대체
- 브라우저에서의 승인 또는 주문 전송

이 경계는 안전장치이자 향후 검증 과제입니다. 기능을 추가할 때는 승인 해시, 선기록 원장, 중복 방지와 비상 정지 계약을 유지해야 합니다.
